# Copy to .env (never committed): cp .env.example .env
# Any variable added to .env must be reported here in the same commit.

POSTGRES_DB=transcendence
POSTGRES_USER=transcendence
POSTGRES_PASSWORD=changeme

# openssl rand -hex 64
JWT_SECRET=changeme
